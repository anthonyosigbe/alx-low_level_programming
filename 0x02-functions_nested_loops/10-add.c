#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result.
 * @a1: first integer to be added.
 * @a2: second integer to be added.
 *
 * Return: result of a1 + a2
 */
int add(int a1, int a2)
{
	return (a1 + a2);
}
